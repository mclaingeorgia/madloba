- if @user[:name]
  = t('mailer.dear', name: @user[:name])
- else
  = t('session.hello')
= t('mailer.post_created', site_name: @site_name)
\:
= t('mailer.post_title')
\: #{@post.title}

\#{t('post.legal_form')}: #{@post.legal_form}

\#{t('admin.items')}
- @post.items.each do |item|
  \- #{item.name}
= t('admin.categories')
- @post.categories.each do |cat|
  \- #{cat.name}
\- #{t('post.beneficiaries_age_group')}: #{@post.benef_age_group}

\#{t('location.location')}:
- @post.locations.each do |location|
  \- #{location.name_and_or_full_address}

= t('mailer.description')
\: #{simple_format(@post.description)}

\------------------------------------------------

\#{t('mailer.manage_post_html', :admin_panel => link_to(t('mailer.admin_panel'), @full_admin_url))}

\#{t('mailer.post_will_expire', day_number: @max_expire_days , expire_date: @post.expire_date )}

\#{t('mailer.thanks')}

\#{t('mailer.signature', name: @site_name)}
