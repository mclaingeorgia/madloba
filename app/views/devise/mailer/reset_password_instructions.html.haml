%p
  = t('session.hello')
  = @resource.first_name
  \!
%p= t('session.someone_requested')
%p= link_to 'Change my password', edit_user_password_url(@resource, reset_password_token: @token, locale: I18n.locale)
%p= t('session.please_ignore')
%p= t('session.password_not_changing')
